public class LRUCache {
    // How do you know least recently used is at the leftmost and most recently used is at the rightmost?(It's not explained in the question)
    // hashmap -> no order
    // linkedlist
    // get -> return value of key, move the node to the rightmost(most recently used)
    // put -> put key and value to the node to the rightmost(most recently used), if full capacity, remove the leftmost node(least recently used)

    
}
